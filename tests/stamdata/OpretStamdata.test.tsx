import {describe, it, expect} from 'vitest';
import '~/pages/field/stamdata/OpretStamdata';

describe('Test opret stamdata', () => {
  it('Check opret lokation', () => {
    expect(true).toBe(true);
  });

  it('Check opret tidsserie/lokation+tidsserie', () => {
    expect(true).toBe(true);
  });

  it('Check at man tilføje udstyr til en oprettet lokation med tidsserie og at man kan oprette lokation+tidsserie + tilføje udstyr på en gang', () => {
    expect(true).toBe(true);
  });

  it('Check at tabs virker korrekt', () => {
    expect(true).toBe(true);
  });
});
