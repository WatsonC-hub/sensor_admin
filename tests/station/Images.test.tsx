import {describe, it, expect} from 'vitest';
import '~/components/ImageCard';
import '~/components/ImageViewer';
import '~/components/Images';
import '~/components/SaveImageDialog';

describe('Test billeder siden på station', () => {
  it('Check at det er muligt at tilføje billeder', () => {
    expect(true).toBe(true);
  });

  it('Check at billeder får den samme størrelse', () => {
    expect(true).toBe(true);
  });

  it('Check at billeder kan slettes', () => {
    expect(true).toBe(true);
  });
});
