import {test, expect} from 'vitest';

import('~/features/opgavestyring/api/useOpgaveStyring');

test('Husk at teste context menu da den kan være påvirket', async () => {
  expect(true).toBe(true);
});

test('kan være påvirket meget', async () => {
  expect(true).toBe(true);
});
