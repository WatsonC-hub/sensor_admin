import {Box, Typography} from '@mui/material';
import React from 'react';

type EmptyRecordsStringProps = {
  value: string;
};

const EmptyRecordsString = ({value}: EmptyRecordsStringProps) => {
  return value;
};

export default EmptyRecordsString;
